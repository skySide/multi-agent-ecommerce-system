spring:
  application:
    name: multi-agent-ecommerce
  ai:
    openai:
      api-key: sk-0Vx0aPGNlkinE0HBB7JEJr9MdlgGvYXOwWxi5pF3D0tj1d77
      base-url: https://api.chatanywhere.tech
      chat:
        options:
          model: gpt-5
          temperature: 0.7
    # Spring AI Milvus 向量数据库配置
    vectorstore:
      milvus:
        host: localhost
        port: 19530
        collection-name: product_vectors
        database-name: default
        index-type: IVF_FLAT
        metric-type: COSINE
        dimension: 768
        initialize-schema: false  # 由 SystemBootstrap 初始化，不自动创建
  datasource:
    url: jdbc:mysql://localhost:3306/ecommerce?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf8
    username: root
    password: lf_MySQL
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: 192.168.252.130
      port: 6379
      password: 123321
      database: 0
      lettuce:
        pool:
          max-active: 10
          max-idle: 10
          min-idle: 1

# MyBatis-Plus 配置
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: auto
  mapper-locations: classpath*:/mapper/**/*.xml

# Embedding 服务配置
embedding:
  service:
    url: http://localhost:8000

server:
  port: 8080
